name: marketpulse
version: "0.1.0"
config-version: 2

profile: marketpulse

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  marketpulse:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table

flags:
  send_anonymous_usage_stats: false

seeds:
  marketpulse:
    known_technologies:
      +column_types:
        technology: varchar
        keyword: varchar
        tech_group: varchar